# Environment variables for the Agent Framework

# PostgreSQL Database Configuration
# Used by the `db` service in docker-compose.yml
POSTGRES_USER=agent_user
POSTGRES_PASSWORD=agent_password
POSTGRES_DB=agent_db

# Full database connection URL
# Used by the `api` and `worker` services to connect to the database
DATABASE_URL=postgresql://agent_user:agent_password@db:5432/agent_db

# OpenAI API Key (or other LLM provider)
# Used by the LLMClient to make API calls
OPENAI_API_KEY="your-api-key-here"
